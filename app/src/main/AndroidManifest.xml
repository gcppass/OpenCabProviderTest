<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <queries>
        <intent>
            <action android:name="com.opencabstandard.VEHICLE_INFORMATION_CHANGED" />
        </intent>
        <intent>
            <action android:name="com.opencabstandard.IDENTITY_INFORMATION_CHANGED" />
        </intent>
    </queries>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpenCapProviderTest"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.OpenCapProviderTest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <service android:name=".OpenCabProviderService" android:exported="true">
            <intent-filter>
                <action android:name="org.opencabstandard.PROVIDER" />
            </intent-filter>
        </service>

        <provider android:authorities="com.example.opencapprovidertest.org.opencabstandard.vehicleinformation"
            android:exported="true"
            android:label="vehicleinformation"
            android:name="com.example.opencapprovidertest.VehicleInformationProvider">
        </provider>
    </application>

</manifest>